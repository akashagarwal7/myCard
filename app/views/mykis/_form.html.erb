<%= form_with(model: myki, local: true) do |form| %>
  <% if myki.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(myki.errors.count, "error") %> prohibited this myki from being saved:</h2>

      <ul>
      <% myki.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number, id: :myki_number %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :myki_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
