<%= form_with(model: passport, local: true) do |form| %>
  <% if passport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(passport.errors.count, "error") %> prohibited this passport from being saved:</h2>

      <ul>
      <% passport.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number, id: :passport_number %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :passport_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
